import { ClearEditorMenuConf, OpenDocumentMenuConf, SaveDocumentMenuConf, SaveDataMenuConf } from './menu/index'

const  documentModule = {
    menus: [ClearEditorMenuConf, OpenDocumentMenuConf, SaveDocumentMenuConf, SaveDataMenuConf],
}

export default documentModule